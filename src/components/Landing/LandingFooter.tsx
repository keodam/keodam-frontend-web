import styles from "./LandingFooter.module.less";

const LandingFooter = () => {
  return (
    <div className={styles.landingFooter}>
      랜딩페지에서 사용할 푸터를 만들 예정입니다.
    </div>
  );
};
export default LandingFooter;
